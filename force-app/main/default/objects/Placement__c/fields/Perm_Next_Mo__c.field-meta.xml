<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Perm_Next_Mo__c</fullName>
    <externalId>false</externalId>
    <formula>if (isnull (Placement_Number__c) || 
ispickval (Retention_Exception__c, &quot;Retention Tracked Under Previous Placement&quot;) || 
not (isnull (One_Year_Retention_End_Date__c)) || 
not (isnull (Case_Record__r.Inactive_Date__c)) || 
Start_Date__c + 366 &lt; date(year(today()-day(today())+32), month(today()-day(today())+32), 1), 0, 

if (ispickval (Job_Type__c, &quot;Permanent&quot;) || ispickval (Job_Type__c, &quot;Temporary-Permanent&quot;) || (ispickval (Job_Type__c, &quot;External Transitional&quot;) &amp;&amp; ispickval (End_Type__c, &quot;Move within Company&quot;)), 

if (month(Start_Date__c + 14) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 14) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 28) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 28) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 49) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 49) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 77) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 77) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 108) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 108) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 140) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 140) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 175) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 175) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 217) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 217) = year(today()-day(today())+32), 1, 0) +  
if (month(Start_Date__c + 266) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 266) = year(today()-day(today())+32), 1, 0) + 
if (month(Start_Date__c + 315) = month(today()-day(today())+32) &amp;&amp; year(Start_Date__c + 315) = year(today()-day(today())+32), 1, 0), 

if (ispickval (Job_Type__c, &quot;External Transitional&quot;), 0, 

0)))</formula>
    <label># Perm, Next Mo</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
