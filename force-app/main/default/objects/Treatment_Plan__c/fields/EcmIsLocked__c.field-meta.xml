<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EcmIsLocked__c</fullName>
    <description>ECM field used to signal to the Service Plan that it is locked and should only show saved targeted Service Plan Indicators and no longer query for the latest outcomes.</description>
    <formula>CONTAINS(TEXT(Status__c), &apos;Final&apos;) || ExpIsLocked__c == true</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>EcmIsLocked</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
